{
  "name": "IntegrationConnection",
  "type": "object",
  "properties": {
    "organization_id": {
      "type": "string"
    },
    "connector_type": {
      "type": "string"
    },
    "provider_workspace_id": {
      "type": "string"
    },
    "connected": {
      "type": "boolean",
      "default": false
    },
    "connection_status": {
      "type": "string",
      "enum": [
        "connected",
        "disconnected",
        "failed",
        "revoked"
      ],
      "default": "disconnected"
    },
    "authentication_status": {
      "type": "string",
      "enum": [
        "valid",
        "expired",
        "revoked",
        "unknown"
      ],
      "default": "unknown"
    },
    "organization_verified": {
      "type": "boolean",
      "default": false
    },
    "capabilities_enabled": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "usage_supported": {
      "type": "boolean",
      "default": false
    },
    "last_sync_started_at": {
      "type": "string",
      "format": "date-time"
    },
    "last_successful_sync_at": {
      "type": "string",
      "format": "date-time"
    },
    "provider_data_through": {
      "type": "string",
      "format": "date-time"
    },
    "provider_data_current_through": {
      "type": "string",
      "format": "date-time"
    },
    "records_processed": {
      "type": "number",
      "default": 0
    },
    "records_created": {
      "type": "number",
      "default": 0
    },
    "records_updated": {
      "type": "number",
      "default": 0
    },
    "usage_events_created": {
      "type": "number",
      "default": 0
    },
    "rate_limit_status": {
      "type": "string",
      "enum": [
        "ok",
        "limited",
        "unknown"
      ],
      "default": "unknown"
    },
    "error_code": {
      "type": "string"
    },
    "error_message": {
      "type": "string"
    },
    "health_status": {
      "type": "string",
      "enum": [
        "healthy",
        "delayed",
        "stale",
        "failed",
        "revoked",
        "unknown"
      ],
      "default": "unknown"
    },
    "revoked_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "organization_id",
    "connector_type"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}