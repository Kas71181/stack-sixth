const steps = ["Connect", "Discover", "Verify", "Decide", "Act"];

export default function ProblemFeatureSections() {
  return <section id="how-it-works" className="mx-auto max-w-7xl px-4 py-20 sm:px-6"><div className="max-w-4xl"><p className="text-sm font-bold uppercase tracking-widest text-primary">The problem</p><h2 className="mt-3 text-3xl font-black sm:text-4xl">Your software stack grew one purchase at a time. Who's managing the whole thing?</h2><p className="mt-4 text-lg leading-8 text-muted-foreground">Subscriptions, owners, costs, renewals, access, and usage signals end up scattered across inboxes, spreadsheets, and teams. Stack Sixth turns that fragmented picture into a decision process.</p></div><div className="mt-10 grid gap-3 sm:grid-cols-5">{steps.map((step,index)=><div key={step} className="rounded-2xl border bg-card p-5"><span className="text-xs font-black text-primary">0{index+1}</span><p className="mt-3 font-bold">{step}</p></div>)}</div></section>;
}