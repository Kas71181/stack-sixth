export default function SignupProgress({ step }) { return <div className="mb-8 flex items-center justify-center gap-2">{["Create Account", "Your Company", "Choose Access"].map((label, index) => <div key={label} className="flex items-center gap-2"><div className={`flex h-7 w-7 items-center justify-center rounded-full text-xs font-bold ${index <= step ? "bg-primary text-primary-foreground" : "bg-muted text-muted-foreground"}`}>{index + 1}</div><span className={`hidden text-xs font-semibold sm:inline ${index <= step ? "text-foreground" : "text-muted-foreground"}`}>{label}</span>{index < 2 && <div className="h-px w-8 bg-border" />}</div>)}</div>; }