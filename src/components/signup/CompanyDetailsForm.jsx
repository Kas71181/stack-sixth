import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
const sizes = ["1-9", "10-49", "50-249", "250-999", "1000+"];
const roles = ["Founder / CEO", "CFO / Finance", "CIO / CTO", "IT Manager / IT Director", "Operations", "Procurement", "Other"];
const industries = ["SaaS", "Agency", "E-commerce", "Healthcare", "Finance", "Professional Services", "Other"];
export default function CompanyDetailsForm({ value, onChange, onNext }) {
  const field = (key, next) => onChange({ ...value, [key]: next });
  return <form onSubmit={(event) => { event.preventDefault(); onNext(); }} className="space-y-5"><div><h1 className="text-2xl font-extrabold">Tell us about your company</h1><p className="mt-1 text-sm text-muted-foreground">We’ll recommend an access level without limiting your choice.</p></div><div className="grid gap-4 sm:grid-cols-2"><div><Label>Company name</Label><Input required value={value.name} onChange={(e) => field("name", e.target.value)} /></div><div><Label>Company size</Label><select value={value.company_size} onChange={(e) => field("company_size", e.target.value)} className="mt-2 h-10 w-full rounded-xl border border-input bg-background px-3 text-sm">{sizes.map((item) => <option key={item}>{item}</option>)}</select></div><div><Label>Industry</Label><select value={value.industry} onChange={(e) => field("industry", e.target.value)} className="mt-2 h-10 w-full rounded-xl border border-input bg-background px-3 text-sm">{industries.map((item) => <option key={item}>{item}</option>)}</select></div><div><Label>Estimated software applications</Label><Input type="number" min="0" value={value.estimated_software_apps} onChange={(e) => field("estimated_software_apps", e.target.value)} placeholder="If known" /></div><div className="sm:col-span-2"><Label>Your role</Label><select value={value.contact_role} onChange={(e) => field("contact_role", e.target.value)} className="mt-2 h-10 w-full rounded-xl border border-input bg-background px-3 text-sm">{roles.map((item) => <option key={item}>{item}</option>)}</select></div></div><button className="w-full rounded-xl bg-primary py-3 text-sm font-bold text-primary-foreground active:scale-[0.96]">Continue to access options</button></form>;
}